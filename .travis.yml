language: php
sudo: false

php:
  - 7.0
fast_finish: true

before_script:
  - composer install -vvv

script:
  - phpunit -v --coverage-clover ./build/logs/clover.xml

after_script:
  -  php bin/coveralls -v